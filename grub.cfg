# GRUB2 Configuration for E93-2026

# Set timeout (seconds to wait before auto-boot)
set timeout=5

# Set default menu entry
set default=0

# Menu entry for the OS
menuentry "E93-2026" {
    # Load the multiboot kernel
    multiboot /boot/kernel.bin
    
    # Boot the kernel
    boot
}
